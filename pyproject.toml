[project]
name = "perch-release-tools"
version = "0.1.0"
description = "Local Python tooling for Perch release scripts"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]
